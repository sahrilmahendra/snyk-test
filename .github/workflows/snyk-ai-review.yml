name: Snyk Code + AI Fix Suggestion

on:
  pull_request:

jobs:
  snyk-ai-review:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

#      - name: Install Snyk CLI
#        run: npm install -g snyk
#
#      - name: Authenticate Snyk CLI
#        run: snyk auth ${{ secrets.SNYK_TOKEN }}

#      - name: Run Snyk Code Test with JSON output
#        id: snyk_scan
#        run: snyk code test --json > snyk-result.json || true

      - name: Run Go Fix Suggestion Script using AI and Post Comment
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
        run: go run scripts/fix_suggestion.go snyk-result.json
